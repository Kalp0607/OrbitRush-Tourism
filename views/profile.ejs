<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head") %>
    <title>My Profile - OrbitRush Tourism</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Inter", sans-serif;
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        min-height: 100vh;
      }

      .profile-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
      }

      .profile-header {
        background: white;
        border-radius: 20px;
        padding: 3rem;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        margin-bottom: 2rem;
        display: flex;
        align-items: center;
        gap: 2rem;
      }

      .profile-avatar {
        position: relative;
      }

      .avatar-image {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid #3b82f6;
        box-shadow: 0 8px 30px rgba(59, 130, 246, 0.3);
      }

      .avatar-badge {
        position: absolute;
        bottom: 5px;
        right: 5px;
        background: #10b981;
        color: white;
        width: 24px;
        height: 24px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.8rem;
        border: 3px solid white;
      }

      .profile-info {
        flex: 1;
      }

      .profile-name {
        font-size: 2.5rem;
        font-weight: 700;
        color: #1a1a1a;
        margin-bottom: 0.5rem;
      }

      .profile-email {
        color: #64748b;
        font-size: 1.1rem;
        margin-bottom: 1rem;
      }

      .profile-actions {
        display: flex;
        gap: 1rem;
      }

      .btn-primary {
        background: linear-gradient(135deg, #3b82f6, #1d4ed8);
        color: white;
        padding: 1rem 2rem;
        border: none;
        border-radius: 12px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 30px rgba(59, 130, 246, 0.4);
        color: white;
      }

      .btn-secondary {
        background: white;
        color: #64748b;
        padding: 1rem 2rem;
        border: 2px solid #e2e8f0;
        border-radius: 12px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
      }

      .btn-secondary:hover {
        border-color: #3b82f6;
        color: #3b82f6;
        transform: translateY(-2px);
      }

      .profile-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
      }

      .profile-card {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
        border: 1px solid #f1f5f9;
      }

      .card-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 1.5rem;
      }

      .card-title {
        font-size: 1.4rem;
        font-weight: 700;
        color: #1a1a1a;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .card-title i {
        color: #3b82f6;
      }

      .recent-enquiries {
        grid-column: 1 / -1;
      }

      .enquiry-item {
        background: #f8fafc;
        padding: 1.5rem;
        border-radius: 12px;
        margin-bottom: 1rem;
        border-left: 4px solid #3b82f6;
      }

      .enquiry-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 1rem;
      }

      .enquiry-tour {
        font-size: 1.2rem;
        font-weight: 600;
        color: #1a1a1a;
      }

      .enquiry-date {
        font-size: 0.9rem;
        color: #64748b;
      }

      .enquiry-details {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1rem;
        margin-top: 1rem;
      }

      .enquiry-detail {
        font-size: 0.9rem;
        color: #64748b;
      }

      .enquiry-detail strong {
        color: #1a1a1a;
      }

      .empty-state {
        text-align: center;
        padding: 3rem 1rem;
        color: #64748b;
      }

      .empty-state i {
        font-size: 3rem;
        margin-bottom: 1rem;
        color: #cbd5e1;
      }

      .quick-action-link {
        display: flex;
        align-items: center;
        padding: 1rem;
        background: #f8fafc;
        border-radius: 12px;
        text-decoration: none;
        color: #1a1a1a;
        transition: all 0.3s ease;
      }

      .quick-action-link:hover {
        background: #f1f5f9;
        transform: translateX(5px);
        color: #1a1a1a;
      }

      @media (max-width: 768px) {
        .profile-header {
          flex-direction: column;
          text-align: center;
        }

        .profile-content {
          grid-template-columns: 1fr;
        }

        .profile-actions {
          justify-content: center;
          flex-wrap: wrap;
        }

        .enquiry-details {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>

  <body>
    <%- include("partials/nav") %>

    <div class="profile-container">
      <!-- Profile Header -->
      <div class="profile-header">
        <div class="profile-avatar">
          <img
            src="<%= user.profileImageURL %>"
            alt="<%= user.fullName %>"
            class="avatar-image"
          />
          <div class="avatar-badge">
            <i class="fas fa-check"></i>
          </div>
        </div>

        <div class="profile-info">
          <h1 class="profile-name"><%= user.fullName %></h1>
          <p class="profile-email">
            <i class="fas fa-envelope"></i> <%= user.email %>
          </p>
        </div>

        <div class="profile-actions">
          <a href="/tour/enquire" class="btn-primary">
            <i class="fas fa-paper-plane"></i>
            New Enquiry
          </a>
        </div>
      </div>

      <!-- Profile Content -->
      <div class="profile-content">
        <!-- Account Information -->
        <div class="profile-card">
          <div class="card-header">
            <h2 class="card-title">
              <i class="fas fa-user-circle"></i>
              Account Information
            </h2>
          </div>

          <div style="space-y: 1rem">
            <div style="margin-bottom: 1rem">
              <label style="font-weight: 600; color: #64748b; font-size: 0.9rem"
                >Full Name</label
              >
              <p style="font-size: 1.1rem; color: #1a1a1a; margin-top: 0.5rem">
                <%= user.fullName %>
              </p>
            </div>

            <div style="margin-bottom: 1rem">
              <label style="font-weight: 600; color: #64748b; font-size: 0.9rem"
                >Email Address</label
              >
              <p style="font-size: 1.1rem; color: #1a1a1a; margin-top: 0.5rem">
                <%= user.email %>
              </p>
            </div>

            <div>
              <label style="font-weight: 600; color: #64748b; font-size: 0.9rem"
                >Member Since</label
              >
              <p style="font-size: 1.1rem; color: #1a1a1a; margin-top: 0.5rem">
                Recently joined
              </p>
            </div>
          </div>
        </div>

        <!-- Quick Actions -->
        <div class="profile-card">
          <div class="card-header">
            <h2 class="card-title">
              <i class="fas fa-bolt"></i>
              Quick Actions
            </h2>
          </div>

          <div style="display: flex; flex-direction: column; gap: 1rem">
            <a href="/tour" class="quick-action-link">
              <i
                class="fas fa-compass"
                style="color: #3b82f6; margin-right: 1rem; width: 20px"
              ></i>
              <div>
                <div style="font-weight: 600">Browse Tours</div>
                <div style="font-size: 0.9rem; color: #64748b">
                  Discover amazing destinations
                </div>
              </div>
            </a>

            <a href="/tour/enquire" class="quick-action-link">
              <i
                class="fas fa-paper-plane"
                style="color: #10b981; margin-right: 1rem; width: 20px"
              ></i>
              <div>
                <div style="font-weight: 600">Make Enquiry</div>
                <div style="font-size: 0.9rem; color: #64748b">
                  Plan your next adventure
                </div>
              </div>
            </a>

            <a href="/contact-us" class="quick-action-link">
              <i
                class="fas fa-headset"
                style="color: #f59e0b; margin-right: 1rem; width: 20px"
              ></i>
              <div>
                <div style="font-weight: 600">Get Support</div>
                <div style="font-size: 0.9rem; color: #64748b">
                  Need help? We're here for you
                </div>
              </div>
            </a>
          </div>
        </div>
      </div>

      <!-- Recent Enquiries -->
      <div class="profile-card recent-enquiries" style="margin-top: 2rem">
        <div class="card-header">
          <h2 class="card-title">
            <i class="fas fa-history"></i>
            Recent Enquiries
          </h2>
          <a
            href="/tour/enquire"
            class="btn-secondary"
            style="padding: 0.5rem 1rem"
          >
            <i class="fas fa-plus"></i>
            New Enquiry
          </a>
        </div>

        <% if (enquiries && enquiries.length > 0) { %> <%
        enquiries.forEach(enquiry => { %>
        <div class="enquiry-item">
          <div class="enquiry-header">
            <div>
              <div class="enquiry-tour"><%= enquiry.tourName %></div>
              <div class="enquiry-date">
                <i class="fas fa-calendar"></i>
                Submitted <%= new Date(enquiry.createdAt).toLocaleDateString()
                %>
              </div>
            </div>
          </div>

          <div class="enquiry-details">
            <div class="enquiry-detail">
              <strong>People:</strong> <%= enquiry.numberOfPeople %>
            </div>
            <div class="enquiry-detail">
              <strong>Preferred Date:</strong>
              <% if (enquiry.preferredDate) { %> <%= new
              Date(enquiry.preferredDate).toLocaleDateString() %> <% } else { %>
              Flexible <% } %>
            </div>
            <div class="enquiry-detail">
              <strong>Phone:</strong> <%= enquiry.phone %>
            </div>
          </div>

          <% if (enquiry.message) { %>
          <div
            style="
              margin-top: 1rem;
              padding-top: 1rem;
              border-top: 1px solid #e2e8f0;
            "
          >
            <div
              style="
                font-size: 0.9rem;
                color: #64748b;
                font-weight: 600;
                margin-bottom: 0.5rem;
              "
            >
              Message:
            </div>
            <p style="color: #1a1a1a; line-height: 1.6">
              <%= enquiry.message %>
            </p>
          </div>
          <% } %>
        </div>
        <% }) %> <% if (enquiries.length === 5) { %>
        <div style="text-align: center; margin-top: 1rem">
          <a href="#" class="btn-secondary">View All Enquiries</a>
        </div>
        <% } %> <% } else { %>
        <div class="empty-state">
          <i class="fas fa-inbox"></i>
          <h3>No enquiries yet</h3>
          <p>
            Start planning your dream vacation by making your first enquiry!
          </p>
          <a href="/tour/enquire" class="btn-primary" style="margin-top: 1rem">
            <i class="fas fa-paper-plane"></i>
            Make Your First Enquiry
          </a>
        </div>
        <% } %>
      </div>
    </div>
    <%- include("partials/footer") %> <%- include("partials/scripts") %>
  </body>
</html>
