<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Booking Confirmed | OrbitRush Tourism</title>
    <%- include("../partials/head") %>
    <style>
      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
      }

      .success-container {
        background: white;
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        width: 100%;
        overflow: hidden;
        animation: slideUp 0.6s ease;
      }

      @keyframes slideUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .success-header {
        background: linear-gradient(135deg, #27ae60, #2ecc71);
        color: white;
        padding: 2.5rem;
        text-align: center;
      }

      .success-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        animation: bounceIn 0.8s ease;
      }

      @keyframes bounceIn {
        0%,
        20%,
        40%,
        60%,
        80% {
          transform: translateY(0);
        }
        50% {
          transform: translateY(-10px);
        }
      }

      .success-content {
        padding: 2.5rem;
      }

      .booking-details {
        background: #f8f9fa;
        border-radius: 15px;
        padding: 2rem;
        margin: 2rem 0;
      }

      .detail-row {
        display: flex;
        justify-content: space-between;
        padding: 0.75rem 0;
        border-bottom: 1px solid #e9ecef;
      }

      .detail-row:last-child {
        border-bottom: none;
        font-weight: 600;
        color: #2c3e50;
      }

      .reference-badge {
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
        padding: 1rem 2rem;
        border-radius: 50px;
        text-align: center;
        margin: 2rem 0;
        font-size: 1.2rem;
        font-weight: 600;
      }

      .action-buttons {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
        margin-top: 2rem;
      }

      .btn {
        padding: 1rem 2rem;
        border: none;
        border-radius: 12px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        text-align: center;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
      }

      .btn-primary {
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
      }

      .btn-outline {
        background: white;
        color: #667eea;
        border: 2px solid #667eea;
      }

      .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      }

      .important-note {
        background: #fff3cd;
        border: 1px solid #ffeaa7;
        border-radius: 10px;
        padding: 1.5rem;
        margin: 2rem 0;
        border-left: 4px solid #f39c12;
      }

      @media (max-width: 768px) {
        .action-buttons {
          grid-template-columns: 1fr;
        }

        .success-header,
        .success-content {
          padding: 2rem 1.5rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="success-container">
      <div class="success-header">
        <div class="success-icon">🎉</div>
        <h1>Booking Confirmed!</h1>
        <p>Your adventure is booked and ready to begin</p>
      </div>

      <div class="success-content">
        <div class="reference-badge">
          <i class="fas fa-ticket-alt"></i>
          Booking Reference: <%= booking.bookingReference %>
        </div>

        <div class="booking-details">
          <h3 style="margin-bottom: 1.5rem; color: #2c3e50">
            <i class="fas fa-info-circle"></i> Your Booking Details
          </h3>

          <div class="detail-row">
            <span>Tour Name:</span>
            <span><%= booking.tourDetails.name %></span>
          </div>
          <div class="detail-row">
            <span>Location:</span>
            <span><%= booking.tourDetails.location %></span>
          </div>
          <div class="detail-row">
            <span>Duration:</span>
            <span><%= booking.tourDetails.duration %></span>
          </div>
          <div class="detail-row">
            <span>Date:</span>
            <span><%= booking.bookingDate.toLocaleDateString('en-IN') %></span>
          </div>
          <div class="detail-row">
            <span>Travelers:</span>
            <span
              ><%= booking.numberOfTravelers %> <%= booking.numberOfTravelers ==
              1 ? 'Person' : 'People' %></span
            >
          </div>
          <div class="detail-row">
            <span>Payment ID:</span>
            <span><%= booking.razorpayPaymentId %></span>
          </div>
          <div class="detail-row">
            <span>Total Amount Paid:</span>
            <span>₹<%= booking.totalAmount.toLocaleString('en-IN') %></span>
          </div>
        </div>

        <div class="important-note">
          <h4 style="margin: 0 0 1rem 0; color: #d68910">
            <i class="fas fa-exclamation-triangle"></i> Important Information
          </h4>
          <ul style="margin: 0; padding-left: 1.2rem; color: #85651d">
            <li>
              A confirmation email has been sent to <%=
              booking.userDetails.email %>
            </li>
            <li>
              Please bring a valid ID and this booking reference on the tour
              date
            </li>
            <li>
              Our team will contact you 24-48 hours before the tour with pickup
              details
            </li>
            <li>For any changes or queries, contact us at +91 98765 43210</li>
          </ul>
        </div>

        <div class="action-buttons">
          <a href="/book/my-bookings" class="btn btn-primary">
            <i class="fas fa-list"></i>
            View My Bookings
          </a>
          <a href="/tour" class="btn btn-outline">
            <i class="fas fa-search"></i>
            Explore More Tours
          </a>
        </div>

        <div style="text-align: center; margin-top: 2rem; color: #6c757d">
          <p><strong>Thank you for choosing OrbitRush Tourism!</strong></p>
          <p>We can't wait to make your travel dreams come true 🌟</p>
        </div>
      </div>
    </div>
  </body>
</html>
