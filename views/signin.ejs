<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head") %>
    <title>Signin</title>
    <style>
      body {
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      .card {
        border: none;
        border-radius: 1rem;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      }
      .form-control {
        border-radius: 0.5rem;
      }
      .btn-primary {
        border-radius: 0.5rem;
        padding: 0.6rem 1.2rem;
        font-weight: 500;
      }
      .signin-header {
        text-align: center;
        font-weight: 600;
        color: #2a5298;
      }
    </style>
  </head>

  <body>
    <%- include("partials/nav") %>

    <div
      class="container d-flex justify-content-center align-items-center flex-grow-1"
    >
      <div class="card p-4 mt-5" style="max-width: 400px; width: 100%">
        <h3 class="signin-header mb-3">Welcome Back 👋</h3>
        <p class="text-center text-muted mb-4">
          Sign in to continue to OrbitRush
        </p>

        <form action="/user/signin" method="post">
          <div class="mb-3">
            <label for="email" class="form-label fw-semibold"
              >Email address</label
            >
            <input
              type="email"
              class="form-control"
              id="exampleInputEmail1"
              name="email"
              aria-describedby="emailHelp"
              placeholder="Enter your email"
              required
            />
            <small id="emailHelp" class="form-text text-muted">
              We'll never share your email with anyone else.
            </small>
          </div>

          <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label fw-semibold"
              >Password</label
            >
            <input
              type="password"
              class="form-control"
              id="exampleInputPassword1"
              placeholder="Enter your password"
              name="password"
              required
            />
          </div>

          <div class="d-grid mt-4">
            <button type="submit" class="btn btn-primary">Sign In</button>
          </div>
          <div class="text-end mt-1">
            <a
              href="/user/forgot-password"
              class="text-decoration-none fw-semibold"
            >
              Forgot Password?
            </a>
          </div>

          <p class="text-center mt-3 mb-0 text-muted">
            Don’t have an account?
            <a href="/user/signup" class="text-decoration-none fw-semibold"
              >Create one</a
            >
          </p>
        </form>
      </div>
    </div>

    <%- include("partials/scripts") %>
  </body>
</html>
