<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />

<!-- Preconnect CDN Resource Hints -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin />
<link rel="preconnect" href="https://cdnjs.cloudflare.com" crossorigin />

<!-- Google Fonts: Plus Jakarta Sans & Outfit for Luxury Travel Experience -->
<link
  href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800;900&family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
  rel="stylesheet"
/>

<!-- Bootstrap 5.3.3 CSS -->
<link
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
  rel="stylesheet"
  integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
  crossorigin="anonymous"
/>

<!-- FontAwesome 6 Icons -->
<link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
  integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
  crossorigin="anonymous"
  referrerpolicy="no-referrer"
/>

<!-- Global Luxury Design System Tokens -->
<style>
  :root {
    --orbit-primary: #0284c7;
    --orbit-primary-dark: #0369a1;
    --orbit-primary-light: #38bdf8;
    --orbit-accent: #f97316;
    --orbit-accent-hover: #ea580c;
    --orbit-dark: #0f172a;
    --orbit-dark-soft: #1e293b;
    --orbit-dark-card: #141e33;
    --orbit-light: #f8fafc;
    --orbit-surface: #ffffff;
    --orbit-surface-muted: #f1f5f9;
    --orbit-border: rgba(226, 232, 240, 0.8);
    --orbit-border-dark: rgba(255, 255, 255, 0.12);
    --orbit-text-main: #0f172a;
    --orbit-text-muted: #64748b;
    --orbit-text-light: #94a3b8;
    --orbit-font-heading: "Outfit", -apple-system, BlinkMacSystemFont, sans-serif;
    --orbit-font-body: "Plus Jakarta Sans", -apple-system, BlinkMacSystemFont, sans-serif;
    --orbit-radius-sm: 10px;
    --orbit-radius-md: 16px;
    --orbit-radius-lg: 24px;
    --orbit-radius-full: 9999px;
    --orbit-shadow-sm: 0 2px 8px rgba(15, 23, 42, 0.04);
    --orbit-shadow-md: 0 10px 30px rgba(15, 23, 42, 0.08);
    --orbit-shadow-lg: 0 20px 50px rgba(15, 23, 42, 0.14);
    --orbit-shadow-glow: 0 0 40px rgba(2, 132, 199, 0.25);
    --orbit-transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: var(--orbit-font-body);
    color: var(--orbit-text-main);
    background-color: var(--orbit-light);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    overflow-x: hidden;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--orbit-font-heading);
    color: var(--orbit-text-main);
    letter-spacing: -0.02em;
    font-weight: 700;
  }

  /* Custom Sleek Scrollbar */
  ::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }
  ::-webkit-scrollbar-track {
    background: #f1f5f9;
  }
  ::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 9999px;
  }
  ::-webkit-scrollbar-thumb:hover {
    background: #94a3b8;
  }
</style>
